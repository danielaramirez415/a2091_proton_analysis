The code reads an image that is conformed of dots. Each dot has an specific color. As an output it will determine which dot have the same color. 
Procedure:
-- import a image (main code)
-- define a grid 
--  
